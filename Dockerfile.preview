# Preview mode - Vite Dev Server
FROM node:18-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
EXPOSE 5173

# Run Vite dev server on all network interfaces
CMD ["npm", "run", "dev", "--", "--host"]
# Note: This Dockerfile is for development preview purposes only.